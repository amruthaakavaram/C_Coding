#include<stdio.h>
int main()
{
    int r;
    printf("enter the value of r:");
    scanf("%d",&r);
    if (r<=20)
    {
        printf("follow this route");
    }
    else
    printf("follow another route");
    return 0;
}